<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidstudio.shi.androidnewwidget">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CoordinatorLayout.CoordinatorLayoutActivity" />
        <activity
            android:name=".Toolbar.ToolBarActivity_01"
            android:theme="@style/NoActionbarTheme" />
        <activity
            android:name=".AppBarLayout.AppBarLayoutActivity"
            android:theme="@style/NoActionbarTheme" />
        <activity android:name=".DrawerLayout.DrawerLayoutActivity" />
        <activity android:name=".StateBar.SystemStateBarColorActivity" />
        <activity android:name=".Toolbar.ToolBarActivity_02"></activity>
    </application>

</manifest>